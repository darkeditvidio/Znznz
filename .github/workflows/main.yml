name: RDP ULTIMATE (مُصلح بالكامل + IP ثابت)

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-2022
    timeout-minutes: 360  # حد GitHub الأقصى 6 ساعات (سيبقى كما هو)

    steps:
      # =========================================================
      # 1. تفعيل RDP وإعداد الجدار الناري
      # =========================================================
      - name: تفعيل RDP
        run: |
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          Restart-Service TermService -Force

      # =========================================================
      # 2. إنشاء مستخدم مدير بكلمة مرور عشوائية
      # =========================================================
      - name: إنشاء مستخدم RDP
        run: |
          $password = "P@ss" + (Get-Random -Maximum 99999) + "!"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "Admin" -Password $securePass -AccountNeverExpires -PasswordNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "Admin"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Admin"
          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

      # =========================================================
      # 3. استرجاع حالة Tailscale من الكاش (لضمان ثبات الـ IP)
      # =========================================================
      - name: استرجاع حالة Tailscale من الكاش
        uses: actions/cache@v4
        id: cache-ts
        with:
          path: C:\ts-state
          key: tailscale-state-${{ runner.os }}
          restore-keys: |
            tailscale-state-${{ runner.os }}

      # =========================================================
      # 4. تثبيت Tailscale
      # =========================================================
      - name: تثبيت Tailscale
        run: |
          # تحميل المثبت
          curl -L -o tailscale.msi https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi
          if (-not (Test-Path tailscale.msi)) {
            Write-Error "فشل تحميل Tailscale"
            exit 1
          }

          # التثبيت بصمت
          Start-Process msiexec.exe -ArgumentList "/i `"$PWD\tailscale.msi`" /quiet /norestart" -Wait
          Start-Sleep -Seconds 5
          Remove-Item tailscale.msi -Force -ErrorAction SilentlyContinue

          # التحقق من التثبيت
          $tsPath = "$env:ProgramFiles\Tailscale\tailscale.exe"
          if (Test-Path $tsPath) {
            Write-Host "✅ تم تثبيت Tailscale بنجاح"
          } else {
            Write-Error "❌ فشل التثبيت"
            exit 1
          }

      # =========================================================
      # 5. الاتصال بـ Tailscale (مع مجلد حالة ثابت)
      # =========================================================
      - name: الاتصال بـ Tailscale
        run: |
          # إنشاء مجلد الحالة إن لم يكن موجوداً (لأول مرة)
          New-Item -ItemType Directory -Force -Path C:\ts-state

          $tsPath = "$env:ProgramFiles\Tailscale\tailscale.exe"
          Write-Host "✅ تم العثور على Tailscale في: $tsPath"

          # الاتصال مع تحديد مجلد الحالة (statedir) لضمان نفس الهوية ونفس الـ IP
          & $tsPath up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-rdp --statedir="C:\ts-state"

          # الحصول على عنوان IP
          $ip = & $tsPath ip -4
          if (-not $ip) {
            Write-Error "لم يتم الحصول على IP"
            exit 1
          }
          echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV

      # =========================================================
      # 6. عرض بيانات الدخول والبقاء شغالاً
      # =========================================================
      - name: عرض بيانات الدخول
        run: |
          Write-Host "====================================================="
          Write-Host "✅ RDP شغال الآن (لمدة 6 ساعات)"
          Write-Host "العنوان: $env:TAILSCALE_IP"
          Write-Host "المستخدم: Admin"
          Write-Host "كلمة المرور: $env:RDP_PASSWORD"
          Write-Host "====================================================="
          Write-Host "⚠️  ملاحظة: الـ IP ثابت ولن يتغير عند إعادة التشغيل"
          Write-Host "    بفضل حفظ حالة Tailscale في الـ Cache."
          Write-Host "====================================================="

          # البقاء شغالاً حتى إلغاء التنفيذ يدوياً
          while ($true) {
            Write-Host "[$(Get-Date)] RDP نشط على IP: $env:TAILSCALE_IP ..."
            Start-Sleep -Seconds 300
          }
